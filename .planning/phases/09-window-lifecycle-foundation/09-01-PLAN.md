---
phase: 09-window-lifecycle-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MyWhisper/Settings/SettingsViewModel.swift
  - MyWhisper/Settings/SettingsView.swift
autonomous: true
requirements:
  - WIN-04
must_haves:
  truths:
    - "SettingsViewModel compila y expone propiedades con didSet para UserDefaults y servicios"
    - "SettingsView renderiza un Form SwiftUI con al menos hotkey recorder y un toggle funcional"
  artifacts:
    - path: "MyWhisper/Settings/SettingsViewModel.swift"
      provides: "Bridge @Observable entre SwiftUI y servicios existentes"
      contains: "@Observable"
    - path: "MyWhisper/Settings/SettingsView.swift"
      provides: "SwiftUI Form placeholder con 1-2 settings reales"
      contains: "KeyboardShortcuts.Recorder"
  key_links:
    - from: "SettingsViewModel"
      to: "UserDefaults.standard"
      via: "didSet en propiedades Bool"
      pattern: "UserDefaults\\.standard\\.set"
    - from: "SettingsViewModel"
      to: "VocabularyService"
      via: "propiedad vocabularyEntries.didSet"
      pattern: "vocabularyService\\.entries"
    - from: "SettingsView"
      to: "SettingsViewModel"
      via: "@Bindable var viewModel"
      pattern: "@Bindable var viewModel"
---

<objective>
Crear SettingsViewModel y SettingsView — los dos archivos nuevos de SwiftUI que servirán como contenido de la ventana de Settings. Incluir 1-2 settings reales (hotkey recorder + toggle de pausar reproduccion) para validar que el keyboard input funciona end-to-end cuando se conecte al NSWindow en Plan 02.

Purpose: Estos archivos son prerequisito del Plan 02 que reescribe SettingsWindowController. Crearlos primero permite compilar y verificar el contenido SwiftUI de forma aislada.
Output: SettingsViewModel.swift y SettingsView.swift en MyWhisper/Settings/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-window-lifecycle-foundation/09-CONTEXT.md
@.planning/research/ARCHITECTURE.md

@MyWhisper/Settings/SettingsWindowController.swift
@MyWhisper/Vocabulary/VocabularyService.swift
@MyWhisper/Vocabulary/VocabularyEntry.swift
@MyWhisper/Audio/MicrophoneDeviceService.swift
@MyWhisper/Cleanup/HaikuCleanupProtocol.swift

<interfaces>
<!-- Tipos y contratos existentes que el executor necesita -->

From MyWhisper/Vocabulary/VocabularyEntry.swift:
```swift
struct VocabularyEntry: Codable, Equatable {
    var wrong: String
    var correct: String
}
```

From MyWhisper/Audio/MicrophoneDeviceService.swift:
```swift
struct AudioDeviceInfo: Identifiable {
    let id: AudioDeviceID
    let name: String
}

final class MicrophoneDeviceService {
    var selectedDeviceID: AudioDeviceID? { get set }
    func availableInputDevices() -> [AudioDeviceInfo]
}
```

From MyWhisper/Vocabulary/VocabularyService.swift:
```swift
final class VocabularyService {
    var entries: [VocabularyEntry] { get set }
}
```

From MyWhisper/Cleanup/HaikuCleanupProtocol.swift:
```swift
protocol HaikuCleanupProtocol: AnyObject, Sendable {
    func clean(_ rawText: String) async throws -> String
    var hasAPIKey: Bool { get async }
    func saveAPIKey(_ key: String) async throws
    func removeAPIKey() async throws
}
```

From MyWhisper/System/HotkeyMonitor.swift:
```swift
extension KeyboardShortcuts.Name {
    static let toggleRecording = Self("toggleRecording", default: .init(.space, modifiers: .option))
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear SettingsViewModel con propiedades parciales para Phase 9</name>
  <files>MyWhisper/Settings/SettingsViewModel.swift</files>
  <read_first>
    - MyWhisper/Settings/SettingsWindowController.swift (ver logica actual de persistencia y servicios)
    - MyWhisper/Vocabulary/VocabularyService.swift (interfaz entries get/set)
    - MyWhisper/Audio/MicrophoneDeviceService.swift (interfaz selectedDeviceID, availableInputDevices, AudioDeviceInfo)
    - MyWhisper/Cleanup/HaikuCleanupProtocol.swift (protocolo para closure de API key)
    - MyWhisper/Vocabulary/VocabularyEntry.swift (tipo VocabularyEntry)
  </read_first>
  <action>
Crear `MyWhisper/Settings/SettingsViewModel.swift` como clase `@Observable @MainActor` (per D-08, D-09 de CONTEXT.md).

El ViewModel debe incluir TODAS las propiedades que necesitara Phase 10, no solo las de Phase 9. Esto evita reescribir el archivo en la siguiente fase. Las propiedades son:

```swift
import Foundation
import CoreAudio
import ServiceManagement

@Observable
@MainActor
final class SettingsViewModel {
    // -- Bool toggles con didSet -> UserDefaults --
    var pausePlaybackEnabled: Bool {
        didSet { UserDefaults.standard.set(pausePlaybackEnabled, forKey: "pausePlaybackEnabled") }
    }
    var maximizeMicVolumeEnabled: Bool {
        didSet { UserDefaults.standard.set(maximizeMicVolumeEnabled, forKey: "maximizeMicVolumeEnabled") }
    }

    // -- Launch at login con SMAppService --
    var launchAtLoginEnabled: Bool {
        didSet {
            do {
                if launchAtLoginEnabled { try SMAppService.mainApp.register() }
                else { try SMAppService.mainApp.unregister() }
            } catch {
                print("[Settings] LaunchAtLogin error: \(error)")
                launchAtLoginEnabled = SMAppService.mainApp.status == .enabled
            }
        }
    }

    // -- Microphone con delegate a service --
    var selectedMicID: AudioDeviceID? {
        didSet { microphoneService.selectedDeviceID = selectedMicID }
    }

    // -- Vocabulary con delegate a service --
    var vocabularyEntries: [VocabularyEntry] {
        didSet { vocabularyService.entries = vocabularyEntries }
    }

    // -- Read-only computed/stored --
    private(set) var availableMics: [AudioDeviceInfo] = []

    // -- Closure para abrir APIKeyWindowController sin importar AppKit --
    var openAPIKey: () -> Void = {}

    // -- Servicios inyectados --
    private let vocabularyService: VocabularyService
    private let microphoneService: MicrophoneDeviceService

    init(vocabularyService: VocabularyService,
         microphoneService: MicrophoneDeviceService,
         haikuCleanup: (any HaikuCleanupProtocol)?) {
        self.vocabularyService = vocabularyService
        self.microphoneService = microphoneService
        // Cargar valores iniciales de la capa de persistencia existente
        self.pausePlaybackEnabled = UserDefaults.standard.bool(forKey: "pausePlaybackEnabled")
        self.maximizeMicVolumeEnabled = UserDefaults.standard.bool(forKey: "maximizeMicVolumeEnabled")
        self.launchAtLoginEnabled = SMAppService.mainApp.status == .enabled
        self.selectedMicID = microphoneService.selectedDeviceID
        self.vocabularyEntries = vocabularyService.entries
        self.availableMics = microphoneService.availableInputDevices()
    }
}
```

NO usar `@AppStorage` — incompatible con `@Observable` (decision de research). Toda persistencia via `didSet` + `UserDefaults.standard.set(...)`.

El parametro `haikuCleanup` se acepta en init para mantener la misma firma que el controller actual pero NO se almacena — el APIKeyWindowController se accede via la closure `openAPIKey`.
  </action>
  <verify>
    <automated>cd /Users/max/Personal/repos/my-superwhisper && xcodebuild -scheme MyWhisper -destination 'platform=macOS' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `MyWhisper/Settings/SettingsViewModel.swift` existe
    - Contiene `@Observable` en la declaracion de clase
    - Contiene `@MainActor` en la declaracion de clase
    - Contiene `var pausePlaybackEnabled: Bool` con `didSet` que llama `UserDefaults.standard.set`
    - Contiene `var maximizeMicVolumeEnabled: Bool` con `didSet` que llama `UserDefaults.standard.set`
    - Contiene `var launchAtLoginEnabled: Bool` con `didSet` que llama `SMAppService.mainApp.register()` / `.unregister()`
    - Contiene `var selectedMicID: AudioDeviceID?` con `didSet` que asigna a `microphoneService.selectedDeviceID`
    - Contiene `var vocabularyEntries: [VocabularyEntry]` con `didSet` que asigna a `vocabularyService.entries`
    - Contiene `var openAPIKey: () -> Void`
    - Contiene `private(set) var availableMics: [AudioDeviceInfo]`
    - Init acepta `vocabularyService: VocabularyService, microphoneService: MicrophoneDeviceService, haikuCleanup: (any HaikuCleanupProtocol)?`
    - NO contiene `@AppStorage`
    - El proyecto compila sin errores
  </acceptance_criteria>
  <done>SettingsViewModel.swift existe con todas las propiedades, didSet bridges, y compila correctamente</done>
</task>

<task type="auto">
  <name>Task 2: Crear SettingsView placeholder con hotkey recorder y toggle</name>
  <files>MyWhisper/Settings/SettingsView.swift</files>
  <read_first>
    - MyWhisper/Settings/SettingsViewModel.swift (recien creado — ver propiedades disponibles)
    - MyWhisper/Settings/SettingsWindowController.swift (ver como se usa KeyboardShortcuts.RecorderCocoa actualmente)
    - MyWhisper/System/HotkeyMonitor.swift (ver la extension KeyboardShortcuts.Name con .toggleRecording)
  </read_first>
  <action>
Crear `MyWhisper/Settings/SettingsView.swift` como SwiftUI View con un Form parcial.

Per D-10 de CONTEXT.md: incluir 1-2 settings reales para validar keyboard input y data binding. Los settings elegidos son:
1. `KeyboardShortcuts.Recorder` — valida keyboard input (WIN-04)
2. `Toggle("Pausar reproduccion al grabar", isOn: $viewModel.pausePlaybackEnabled)` — valida data binding bidireccional

El Form debe usar `.formStyle(.grouped)` per la research (estilo System Settings de macOS).

```swift
import SwiftUI
import KeyboardShortcuts

struct SettingsView: View {
    @Bindable var viewModel: SettingsViewModel

    var body: some View {
        Form {
            Section("Grabacion") {
                KeyboardShortcuts.Recorder("Atajo de grabacion:", name: .toggleRecording)
                Toggle("Pausar reproduccion al grabar", isOn: $viewModel.pausePlaybackEnabled)
            }
        }
        .formStyle(.grouped)
        .frame(minWidth: 420, minHeight: 200)
    }
}
```

Notas importantes:
- Usar `KeyboardShortcuts.Recorder` (SwiftUI variant), NO `RecorderCocoa` — per Pitfall 4 del research
- Usar `@Bindable var viewModel` para derivar bindings de la clase `@Observable`
- `.frame(minWidth: 420, minHeight: 200)` establece tamanio minimo razonable para el Form parcial
- Las secciones restantes (API, Vocabulario, Sistema) se agregan en Phase 10 (per D-11)
  </action>
  <verify>
    <automated>cd /Users/max/Personal/repos/my-superwhisper && xcodebuild -scheme MyWhisper -destination 'platform=macOS' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `MyWhisper/Settings/SettingsView.swift` existe
    - Contiene `import KeyboardShortcuts`
    - Contiene `@Bindable var viewModel: SettingsViewModel`
    - Contiene `KeyboardShortcuts.Recorder("Atajo de grabacion:", name: .toggleRecording)` (SwiftUI variant, NO RecorderCocoa)
    - Contiene `Toggle("Pausar reproduccion al grabar", isOn: $viewModel.pausePlaybackEnabled)`
    - Contiene `.formStyle(.grouped)`
    - Contiene `.frame(minWidth:`
    - NO contiene `RecorderCocoa`
    - NO contiene `import AppKit`
    - El proyecto compila sin errores
  </acceptance_criteria>
  <done>SettingsView.swift existe con Form parcial, hotkey recorder SwiftUI y toggle funcional, compila correctamente</done>
</task>

</tasks>

<verification>
- `xcodebuild -scheme MyWhisper -destination 'platform=macOS' build` compila sin errores
- Los dos archivos nuevos existen en MyWhisper/Settings/
- SettingsViewModel tiene todas las propiedades con didSet bridges
- SettingsView usa KeyboardShortcuts.Recorder (SwiftUI) y un Toggle con binding
</verification>

<success_criteria>
- SettingsViewModel.swift y SettingsView.swift creados y compilando
- SettingsViewModel expone todas las propiedades necesarias con didSet persistence
- SettingsView contiene un Form con al menos hotkey recorder y un toggle
- Sin errores de compilacion en el proyecto completo
</success_criteria>

<output>
After completion, create `.planning/phases/09-window-lifecycle-foundation/09-01-SUMMARY.md`
</output>
