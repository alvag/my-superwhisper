---
phase: 04-settings-history-and-polish
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - MyWhisper/App/AppDelegate.swift
  - MyWhisper/UI/StatusMenuView.swift
autonomous: false
requirements: [MAC-05]

must_haves:
  truths:
    - "App idle RAM is profiled and documented after STT model load"
    - "MAC-05 status is determined — passes (<200MB RSS) or documented with explanation"
  artifacts:
    - path: ".planning/phases/04-settings-history-and-polish/04-03-SUMMARY.md"
      provides: "RAM profiling results and MAC-05 determination"
      contains: "MAC-05"
  key_links: []
---

<objective>
Profile idle RAM usage to verify MAC-05 (<200MB), document findings, and perform final human verification of all Phase 4 features. This is primarily a measurement task — WhisperKit model memory behavior on Apple Silicon may report differently depending on where CoreML allocates (RSS vs Neural Engine).

Purpose: Resolves the highest-risk open question for Phase 4. If RAM exceeds budget, documents the finding with recommendations. Final checkpoint ensures all features work end-to-end.

Output: RAM profiling results in SUMMARY, MAC-05 determination, user sign-off on Phase 4.
</objective>

<execution_context>
@/Users/max/.claude/get-shit-done/workflows/execute-plan.md
@/Users/max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-settings-history-and-polish/04-CONTEXT.md
@.planning/phases/04-settings-history-and-polish/04-RESEARCH.md
@.planning/phases/04-settings-history-and-polish/04-01-SUMMARY.md
@.planning/phases/04-settings-history-and-polish/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile idle RAM and build release archive</name>
  <files>
    (no new source files — profiling task)
  </files>
  <read_first>
    MyWhisper/STT/STTEngine.swift,
    MyWhisper/App/AppDelegate.swift,
    .planning/phases/04-settings-history-and-polish/04-RESEARCH.md
  </read_first>
  <action>
    1. Build a release version:
       ```bash
       xcodebuild build -scheme MyWhisper -configuration Release -quiet
       ```

    2. Profile idle RAM using command line:
       ```bash
       # Find the built app and launch it
       # Wait 30 seconds for STT model to load
       # Use `ps` or `top` to check RSS:
       ps -o rss,vsz,comm -p $(pgrep -f MyWhisper) | head -5
       ```
       Note: This requires the app to launch, which may not be possible in CI. If the app cannot be launched (no display server), use `xcodebuild build` size analysis and document that runtime profiling requires manual verification.

    3. Document findings:
       - If RSS < 200MB: MAC-05 passes
       - If RSS > 200MB: Document that CoreML model memory on Apple Silicon is allocated in unified memory managed by the Neural Engine, which may not appear in process RSS. Note: the 200MB budget likely refers to app process memory excluding CoreML model buffers. Document the exact RSS observed and recommend whether to:
         a) Accept the current model (large-v3-turbo) with the caveat that CoreML memory is separate
         b) Switch to whisper-small (~180MB) to meet the strict budget
         c) Re-scope MAC-05 to "app process RSS excluding CoreML model memory"
  </action>
  <acceptance_criteria>
    - `xcodebuild build -scheme MyWhisper -configuration Release -quiet` exits 0
    - SUMMARY.md documents RAM profiling results or documents that runtime profiling requires manual step
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -scheme MyWhisper -configuration Release -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Release build succeeds
    - RAM profiling documented with specific numbers or explicit note that manual profiling needed
    - MAC-05 status determined (pass/conditional/needs-manual-verification)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full Phase 4 verification</name>
  <files></files>
  <action>User verifies all Phase 4 features end-to-end.</action>
  <what-built>Complete Phase 4 features: Settings panel (hotkey recorder, mic selector, API key, vocabulary table, launch-at-login), History panel (click-to-copy), vocabulary corrections in pipeline, KeyboardShortcuts migration, and RAM profiling.</what-built>
  <how-to-verify>
    1. Launch MyWhisper from Xcode (Cmd+R)
    2. Click menubar icon -> "Preferencias..." -> verify Settings panel opens with all 5 sections
    3. In Settings: click hotkey recorder field, press a new key combo (e.g., Cmd+Shift+R), verify it displays
    4. In Settings: verify microphone dropdown shows available audio devices
    5. In Settings: click [+] to add vocabulary entry, type "cluad" -> "Claude", verify row appears
    6. Close Settings, press hotkey to record, speak, verify transcription contains vocabulary correction if applicable
    7. Click menubar icon -> "Historial" -> verify history window shows the transcription with timestamp
    8. Click a history entry -> verify text is copied to clipboard (Cmd+V to paste somewhere to confirm)
    9. Check Activity Monitor for MyWhisper RSS at idle (after model loads, ~30s)
    10. Verify RAM reading and compare against 200MB target
  </how-to-verify>
  <verify>User confirms all features work</verify>
  <done>User types "approved"</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Release build succeeds
- User confirms all Phase 4 features work as described
- RAM budget documented
</verification>

<success_criteria>
- All Phase 4 features functional: settings, history, vocabulary, hotkey config, mic selection
- MAC-05 profiled and documented
- User has verified the complete end-to-end experience
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings-history-and-polish/04-03-SUMMARY.md`
</output>
