---
phase: 09-window-lifecycle-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - MyWhisper/Settings/SettingsWindowController.swift
autonomous: false
requirements:
  - WIN-01
  - WIN-02
  - WIN-03
  - WIN-04
must_haves:
  truths:
    - "La ventana de Settings permanece abierta cuando el usuario hace click fuera de ella (WIN-01)"
    - "La ventana de Settings se cierra solo con el boton X o Cmd+W (WIN-02)"
    - "Al cerrar Settings, el dock icon desaparece y el foco vuelve a la app anterior (WIN-03)"
    - "La ventana de Settings acepta input de teclado en TextFields y hotkey recorder (WIN-04)"
  artifacts:
    - path: "MyWhisper/Settings/SettingsWindowController.swift"
      provides: "NSWindow host con NSHostingController, activation policy lifecycle"
      contains: "NSWindow(contentViewController:"
  key_links:
    - from: "SettingsWindowController.show()"
      to: "NSApp.setActivationPolicy(.regular)"
      via: "llamada antes de makeKeyAndOrderFront"
      pattern: "setActivationPolicy\\(.regular\\)"
    - from: "SettingsWindowController.windowShouldClose"
      to: "NSApp.setActivationPolicy(.accessory)"
      via: "delegate callback en cierre"
      pattern: "setActivationPolicy\\(.accessory\\)"
    - from: "SettingsWindowController.windowShouldClose"
      to: "NSApp.hide(nil)"
      via: "llamada despues de restaurar .accessory para devolver foco"
      pattern: "NSApp\\.hide\\(nil\\)"
    - from: "SettingsWindowController"
      to: "NSHostingController(rootView: SettingsView)"
      via: "contentViewController del NSWindow"
      pattern: "NSHostingController"
    - from: "SettingsWindowController"
      to: "SettingsViewModel.openAPIKey"
      via: "closure injection en init"
      pattern: "viewModel\\.openAPIKey"
---

<objective>
Reescribir SettingsWindowController para reemplazar NSPanel con NSWindow + NSHostingController. La ventana de Settings debe permanecer abierta cuando el usuario hace click fuera de ella, aceptar keyboard input, cerrarse solo con X/Cmd+W, y restaurar limpiamente el activation policy al cerrar.

Purpose: Este es el cambio core de Phase 9 — resuelve el bug de que Settings se cierra al perder foco (WIN-01) y establece la base para Phase 10.
Output: SettingsWindowController.swift reescrito (~60 lineas vs las 305 actuales)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-window-lifecycle-foundation/09-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/09-window-lifecycle-foundation/09-01-SUMMARY.md

@MyWhisper/Settings/SettingsWindowController.swift
@MyWhisper/Settings/SettingsViewModel.swift
@MyWhisper/Settings/SettingsView.swift
@MyWhisper/UI/StatusMenuView.swift
@MyWhisper/App/AppDelegate.swift

<interfaces>
<!-- Contratos creados por Plan 01 que este plan consume -->

From MyWhisper/Settings/SettingsViewModel.swift:
```swift
@Observable
@MainActor
final class SettingsViewModel {
    var pausePlaybackEnabled: Bool  // didSet -> UserDefaults
    var maximizeMicVolumeEnabled: Bool  // didSet -> UserDefaults
    var launchAtLoginEnabled: Bool  // didSet -> SMAppService
    var selectedMicID: AudioDeviceID?  // didSet -> microphoneService
    var vocabularyEntries: [VocabularyEntry]  // didSet -> vocabularyService
    private(set) var availableMics: [AudioDeviceInfo]
    var openAPIKey: () -> Void

    init(vocabularyService: VocabularyService,
         microphoneService: MicrophoneDeviceService,
         haikuCleanup: (any HaikuCleanupProtocol)?)
}
```

From MyWhisper/Settings/SettingsView.swift:
```swift
struct SettingsView: View {
    @Bindable var viewModel: SettingsViewModel
    // Form con KeyboardShortcuts.Recorder + Toggle
}
```

From MyWhisper/UI/StatusMenuView.swift:
```swift
// openSettings() crea SettingsWindowController con la misma firma de init:
settingsWindowController = SettingsWindowController(
    vocabularyService: vocab,
    microphoneService: mic,
    haikuCleanup: haikuCleanup
)
settingsWindowController?.show()
```

From MyWhisper/App/AppDelegate.swift (precedente NSWindow + NSHostingView):
```swift
// showPermissionBlockedWindow — patron existente a seguir:
let window = NSWindow(
    contentRect: NSRect(x: 0, y: 0, width: 420, height: 300),
    styleMask: [.titled, .closable],
    backing: .buffered,
    defer: false
)
window.isReleasedWhenClosed = false
NSApp.setActivationPolicy(.regular)
NSApp.activate(ignoringOtherApps: true)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir SettingsWindowController — NSPanel a NSWindow + NSHostingController</name>
  <files>MyWhisper/Settings/SettingsWindowController.swift</files>
  <read_first>
    - MyWhisper/Settings/SettingsWindowController.swift (codigo actual completo — 305 lineas a reemplazar)
    - MyWhisper/Settings/SettingsViewModel.swift (creado en Plan 01 — ver init signature)
    - MyWhisper/Settings/SettingsView.swift (creado en Plan 01 — ver constructor)
    - MyWhisper/UI/StatusMenuView.swift (ver como se instancia el controller en openSettings)
    - MyWhisper/App/AppDelegate.swift lineas 122-139 (precedente de NSWindow + NSHostingView)
    - MyWhisper/UI/APIKeyWindowController.swift (si existe — ver interfaz show())
  </read_first>
  <action>
Reescribir COMPLETAMENTE `MyWhisper/Settings/SettingsWindowController.swift`. Eliminar todo el codigo AppKit de UI (NSTableView, NSLayoutConstraint, NSPopUpButton, NSCheckbox, target/action). Reemplazar con NSWindow + NSHostingController hosting.

El archivo resultante debe implementar esto (per D-01 a D-09 de CONTEXT.md y ARCHITECTURE.md):

```swift
import AppKit
import SwiftUI

@MainActor
final class SettingsWindowController: NSObject, NSWindowDelegate {
    private var window: NSWindow?
    private let viewModel: SettingsViewModel
    private var apiKeyWindowController: APIKeyWindowController?

    init(vocabularyService: VocabularyService,
         microphoneService: MicrophoneDeviceService,
         haikuCleanup: (any HaikuCleanupProtocol)?) {
        self.viewModel = SettingsViewModel(
            vocabularyService: vocabularyService,
            microphoneService: microphoneService,
            haikuCleanup: haikuCleanup
        )
        self.apiKeyWindowController = APIKeyWindowController(haikuCleanup: haikuCleanup)
        super.init()
        self.viewModel.openAPIKey = { [weak self] in
            self?.apiKeyWindowController?.show()
        }
    }

    func show() {
        if let existing = window {
            NSApp.setActivationPolicy(.regular)
            existing.makeKeyAndOrderFront(nil)
            NSApp.activate(ignoringOtherApps: true)
            return
        }

        let hostingController = NSHostingController(rootView: SettingsView(viewModel: viewModel))
        let window = NSWindow(contentViewController: hostingController)
        window.title = "MyWhisper — Preferencias"
        window.isReleasedWhenClosed = false
        window.styleMask = [.titled, .closable]
        window.delegate = self
        window.center()

        NSApp.setActivationPolicy(.regular)
        window.makeKeyAndOrderFront(nil)
        NSApp.activate(ignoringOtherApps: true)

        self.window = window
    }

    // MARK: - NSWindowDelegate

    func windowShouldClose(_ sender: NSWindow) -> Bool {
        sender.orderOut(nil)
        NSApp.setActivationPolicy(.accessory)
        NSApp.hide(nil)
        return false
    }
}
```

Cambios criticos respecto al codigo actual:
1. `NSPanel` -> `NSWindow` (per D-01) — NSWindow no tiene hidesOnDeactivate, resuelve WIN-01
2. `private var panel: NSPanel?` -> `private var window: NSWindow?`
3. Eliminar TODAS las conformances: NSTableViewDataSource, NSTableViewDelegate
4. Eliminar TODO: NSLayoutConstraint, NSPopUpButton, NSCheckbox, NSButton, NSScrollView, NSTableView, vocabEntries, tableView ref
5. Eliminar TODOS los @objc handlers: microphoneChanged, changeAPIKey, addVocabEntry, removeVocabEntry, launchAtLoginChanged, pausePlaybackChanged, maximizeMicVolumeChanged
6. Eliminar numberOfRows y ambos tableView(_:objectValueFor:) y tableView(_:setObjectValue:)
7. Agregar `NSApp.hide(nil)` en close handler (per D-06) — el codigo actual NO lo tiene
8. Agregar `NSApp.setActivationPolicy(.regular)` en la rama re-show — el codigo actual no lo hace
9. Usar `windowShouldClose` (retorna false) + `orderOut(nil)` en lugar de `windowWillClose` + `self.panel = nil` — per D-03 que dice preservar instancia con orderOut

La firma de init se mantiene identica: `init(vocabularyService:microphoneService:haikuCleanup:)` — StatusMenuController no necesita cambios.
  </action>
  <verify>
    <automated>cd /Users/max/Personal/repos/my-superwhisper && xcodebuild -scheme MyWhisper -destination 'platform=macOS' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SettingsWindowController.swift tiene menos de 80 lineas
    - NO contiene `NSPanel` en ninguna parte
    - NO contiene `NSTableView` en ninguna parte
    - NO contiene `NSLayoutConstraint` en ninguna parte
    - NO contiene `NSPopUpButton` en ninguna parte
    - NO contiene `NSTableViewDataSource` ni `NSTableViewDelegate` en la declaracion de clase
    - Contiene `NSWindow(contentViewController:` (usa NSHostingController)
    - Contiene `NSHostingController(rootView: SettingsView(viewModel:`
    - Contiene `isReleasedWhenClosed = false`
    - Contiene `NSApp.setActivationPolicy(.regular)` en show()
    - Contiene `NSApp.setActivationPolicy(.accessory)` en windowShouldClose
    - Contiene `NSApp.hide(nil)` en windowShouldClose
    - Contiene `NSApp.activate(ignoringOtherApps: true)` en show()
    - Contiene `viewModel.openAPIKey` closure assignment
    - Contiene `windowShouldClose` que retorna `false`
    - Contiene `orderOut(nil)`
    - Init acepta la misma firma: `vocabularyService: VocabularyService, microphoneService: MicrophoneDeviceService, haikuCleanup: (any HaikuCleanupProtocol)?`
    - El proyecto compila sin errores
  </acceptance_criteria>
  <done>SettingsWindowController.swift reescrito con NSWindow + NSHostingController, activation policy lifecycle completo, compila sin errores</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar comportamiento de ventana de Settings</name>
  <files>MyWhisper/Settings/SettingsWindowController.swift</files>
  <action>
Verificacion manual del ciclo de vida completo de la ventana de Settings. El usuario debe ejecutar la app (Cmd+R en Xcode) y probar los 5 escenarios listados en how-to-verify. Ningun cambio de codigo es necesario — esto es solo verificacion.
  </action>
  <what-built>
    Ventana de Settings completamente reescrita: NSPanel reemplazado con NSWindow + NSHostingController.
    La ventana ahora muestra un SwiftUI Form con hotkey recorder y toggle de pausar reproduccion.
    El ciclo de vida de activacion (dock icon, foco, keyboard input) esta implementado.
  </what-built>
  <how-to-verify>
    Ejecutar la app (Cmd+R en Xcode) y verificar:

    1. **WIN-01 — Ventana persiste al click fuera:**
       - Click en el icono del menubar -> "Preferencias..."
       - La ventana de Settings aparece
       - Click en el escritorio o en otra app
       - ESPERADO: La ventana de Settings sigue visible (NO se cierra)

    2. **WIN-02 — Cierre solo con X o Cmd+W:**
       - Con Settings abierta, presionar Cmd+W
       - ESPERADO: La ventana se cierra/oculta
       - Reabrir Settings y cerrar con el boton rojo X
       - ESPERADO: La ventana se cierra/oculta

    3. **WIN-03 — Dock icon desaparece y foco restaurado:**
       - Abrir una app (ej: TextEdit), escribir algo
       - Abrir Settings desde menubar
       - ESPERADO: Dock icon de MyWhisper aparece
       - Cerrar Settings con Cmd+W
       - ESPERADO: Dock icon de MyWhisper desaparece
       - ESPERADO: TextEdit recupera el foco (puedes seguir escribiendo)

    4. **WIN-04 — Keyboard input funciona:**
       - Abrir Settings
       - Click en el hotkey recorder "Atajo de grabacion:"
       - ESPERADO: El recorder entra en modo "esperando shortcut"
       - Presionar una combinacion de teclas
       - ESPERADO: El recorder muestra la nueva combinacion

    5. **Reabrir sin duplicados:**
       - Cerrar Settings, reabrir desde menubar
       - ESPERADO: La ventana aparece sin crear duplicados
       - ESPERADO: Los valores anteriores (toggle, hotkey) se mantienen
  </how-to-verify>
  <verify>Verificacion manual — el usuario ejecuta los 5 escenarios</verify>
  <done>Los 5 escenarios pasan: ventana persiste, cierra con X/Cmd+W, dock icon restaurado, keyboard funciona, reabrir sin duplicados</done>
  <resume-signal>Escribe "aprobado" si todo funciona, o describe los problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- La app compila y ejecuta sin errores ni warnings en consola relacionados con constraints
- Settings se abre desde el menubar
- Click fuera de Settings no cierra la ventana
- Cmd+W y boton X cierran la ventana
- Dock icon aparece al abrir y desaparece al cerrar
- Keyboard input funciona en el hotkey recorder
- Toggle de pausa funciona y persiste al reabrir
</verification>

<success_criteria>
- WIN-01: Ventana persiste al click fuera — verificado manualmente
- WIN-02: Solo cierra con X o Cmd+W — verificado manualmente
- WIN-03: Dock icon desaparece y foco restaurado — verificado manualmente
- WIN-04: Keyboard input funciona — verificado manualmente con hotkey recorder
- El proyecto compila sin errores
- StatusMenuController no requirio cambios (misma firma de init)
</success_criteria>

<output>
After completion, create `.planning/phases/09-window-lifecycle-foundation/09-02-SUMMARY.md`
</output>
